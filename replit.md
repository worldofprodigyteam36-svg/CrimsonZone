# CrimsonZone on Replit

## Run the app

The `Start application` workflow runs:

```sh
PORT=5000 HOST=0.0.0.0 npm start
```

The Express server serves the frontend, JSON APIs, Ultraviolet assets, and the Wisp WebSocket endpoint from the same port.

No additional secrets are required for basic startup.